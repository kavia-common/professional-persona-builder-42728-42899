# Optional: base URL for the Express backend API.
# - If unset, the frontend will use same-origin requests (""), which works behind a reverse proxy.
# - In local dev with separate ports, set e.g. http://localhost:3001
VITE_EXPRESS_BACKEND_URL=http://localhost:3001
